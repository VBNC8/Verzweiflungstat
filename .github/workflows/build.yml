name: Build Hector-wireless
on: [push, pull_request, workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: zmkfirmware/zmk-build-arm:4.1
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: West Init
        run: west init -l config

      - name: West Update
        run: west update
      - name: West Zephyr Export
        run: west zephyr-export
      - name: Build Left
        run: west build -s zmk/app -b nice_nano_v2//zmk -- -DSHIELD="supermini_split_left" -DZMK_CONFIG="${GITHUB_WORKSPACE}/config"
      - name: Save Left
        run: cp build/zephyr/zmk.uf2 hector-wireless-left.uf2
      - name: Build Right
        run: west build -p -s zmk/app -b nice_nano_v2//zmk -- -DSHIELD="supermini_split_right" -DZMK_CONFIG="${GITHUB_WORKSPACE}/config"
      - name: Save Right
        run: cp build/zephyr/zmk.uf2 hector-wireless-right.uf2
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: hector-wireless-firmware
          path: |
            hector-wireless-left.uf2
            hector-wireless-right.uf2
