// Root overlay — reference / documentation only.
// The actual UART DTS configuration lives in the per-side shield overlays
// (handwired_left.overlay and handwired_right.overlay).
//
// GP8 = UART0 TX  (connect to the other half's RX)
// GP9 = UART0 RX  (connect to the other half's TX)
