/* Wir schalten SPI komplett aus, um CPU-Zyklen zu sparen */
&spi1 {
    status = "disabled";
};

/* Wir sorgen dafür, dass P0.06 (dein DR-Pin) nicht als Interrupt registriert ist */
/delete-node/ &trackpad;
