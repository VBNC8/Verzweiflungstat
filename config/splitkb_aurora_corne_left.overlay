/ {
    chosen {
        zmk,backlight = &backlight;
    };

    backlight: backlight {
        compatible = "zmk,backlight-gpio"; // Geändert von "gpio-backlight"
        gpios = <&gpio0 29 GPIO_ACTIVE_HIGH>;
    };
};

&left_encoder {
    status = "okay";
};
