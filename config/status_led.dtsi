// Nur die Hardware-Zuordnung
/ {
    leds {
        compatible = "gpio-leds";
        status_led_node: led_0 {
            gpios = <&gpio0 29 GPIO_ACTIVE_HIGH>;
            label = "Status LED";
        };
    };
};
