name: Build
on: [push, pull_request, workflow_dispatch]
jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      build_matrix: |
        [
          {
            "board": "supermini_nrf52840",
            "shield": "supermini_split_left"
          },
          {
            "board": "supermini_nrf52840",
            "shield": "supermini_split_right"
          }
        ]
