# Wired split – RIGHT HALF (peripheral, no direct USB to host)
#
# The right side sends key events to the central (left half) via UART.

CONFIG_ZMK_SPLIT=y

# UART serial transport for wired split
CONFIG_SERIAL=y
CONFIG_UART_INTERRUPT_DRIVEN=y

# Idle timeout (30 s)
CONFIG_ZMK_IDLE_TIMEOUT=30000
