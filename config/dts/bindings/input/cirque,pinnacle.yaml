# Falls die Datei noch nicht so anfängt:
description: "Cirque Pinnacle"
compatible: "cirque-pinnacle"

description: "Cirque Pinnacle Trackpad"
compatible: "cirque,pinnacle"
include: spi-device.yaml

properties:
  dr-gpios:
    type: phandle-array
    required: true
    description: "Data Ready Interrupt GPIO"
