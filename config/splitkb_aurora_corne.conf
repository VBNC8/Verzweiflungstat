CONFIG_ZMK_USB_BOOT=y

# --- ENCODER ---
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# --- VERBINDUNG ---
# Standard TX-Leistung (0 dBm) statt +8 dBm spart deutlich Energie.
# Bei Verbindungsproblemen kann auf PLUS_4 oder PLUS_8 erhöht werden.
CONFIG_BT_CTLR_TX_PWR_0=y

# --- ENERGIESPAREN ---
# Nach 30 s Inaktivität BLE-Aktivität reduzieren (Standard-ZMK-Wert).
CONFIG_ZMK_IDLE_TIMEOUT=30000
# CONFIG_ZMK_IDLE_SLEEP_TIMEOUT ist deaktiviert: der Tiefsschlaf-Übergang
# führt bei aktiviertem Display (nice!view + dedizierter Work Queue) zu einem
# Deadlock, bei dem das Display eingefroren bleibt und keine Tasten mehr
# registriert werden.  Nur im USB-Betrieb tritt das Problem nicht auf, weil
# ZMK dort den Schlaf-Pfad überspringt.

# --- BATTERIE ---
CONFIG_ZMK_EXT_POWER=y
CONFIG_ZMK_BATTERY_REPORTING=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=y

# --- DISPLAY (nice!view) ---
CONFIG_ZMK_DISPLAY=y

# DISABLE diese Standard-Widgets (sie verursachen den Fehler!)
CONFIG_ZMK_WIDGET_BATTERY_STATUS=n
CONFIG_ZMK_WIDGET_OUTPUT_STATUS=n

# ENABLE das nice!view-spezifische Widget
CONFIG_NICE_VIEW_WIDGET_STATUS=y

# Hilfreich für die Performance
CONFIG_ZMK_DISPLAY_WORK_QUEUE_DEDICATED=y
