# Maus-Funktionen für Scrollen
CONFIG_ZMK_MOUSE=y
CONFIG_ZMK_POINTING=y
CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=n
# Erzwingt die USB-Maus-Meldung beim Host
CONFIG_ZMK_USB_BOOT=y

# Encoder
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# RGB Underglow
CONFIG_ZMK_RGB_UNDERGLOW=y
CONFIG_WS2812_STRIP=y

# Verbindung optimieren
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# --- DISPLAY FIX ---
CONFIG_ZMK_DISPLAY=y

# Wir schalten das Shield-Widget EIN (damit es hell wird)
CONFIG_NICE_VIEW_WIDGET_STATUS=y

# Wir schalten nur die WPM-Anzeige innerhalb von ZMK aus
# Das Symbol muss ZMK_WIDGET_WPM_STATUS heißen
CONFIG_ZMK_WIDGET_WPM_STATUS=n

# Falls vorhanden, schalten wir die Batterie-Anzeige ein
CONFIG_ZMK_WIDGET_BATTERY_STATUS=y
CONFIG_ZMK_WIDGET_BATTERY_STATUS_SHOW_PERCENTAGE=y
